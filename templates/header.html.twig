<div class="container-fluid p-4 bg-transparent text-white text-right">
    <video autoplay loop muted style="z-index: -1">
        <source src="{{ asset('videos/header_video.mp4') }}" type="video/mp4">
    </video>
    <a href="http://localhost:8000/"><img src="/images/surf_icon.png" alt="surf icon" style="width: 80px; height: 80px; position: absolute; top: 0; left: 0; margin: 0; padding: 0;"></a>
    <button type="button" class="btn btn-secondary bg-dark" onclick="location.href = 'http://localhost:8000/';">Home</button>
    <button class="btn btn-secondary bg-dark" onclick="location.href = 'http://localhost:8000/restaurant/web';">Restaurant</button>
    <button class="btn btn-secondary bg-dark" onclick="location.href = 'http://localhost:8000/login';">Login</button>
    <button class="btn btn-secondary bg-dark" onclick="location.href = 'http://localhost:8000/register';">Register</button>
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <a href="{{ path('app_logout') }}"  class="btn btn-secondary bg-dark">Logout</a>
    {% endif %}

    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <button type="button" class="btn btn-secondary bg-dark" onclick="location.href = 'http://localhost:8000/news';">
            News
        </button>

    {% endif %}

</div>
<center><h2>WELCOME TO THE SURFING CLUB!</h2></center>       
